# 使用较旧版本的 akshare 以避免构建问题
# 如果安装失败，请尝试：pip install akshare==1.14.0
akshare>=1.12.0,<1.16.0
mcp>=1.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# 缓存支持
cachetools>=5.0.0

# 速率限制支持
pyrate-limiter>=3.0.0

# 如果 curl-cffi 构建失败，请先安装：
# brew install curl-impersonate
# 或使用预编译版本：pip install curl-cffi==0.6.0
